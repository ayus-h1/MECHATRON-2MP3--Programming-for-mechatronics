// long out = pop(&start, &stop);
    // printf("popped %ld\n", out);
    // printStack(&start, &stop);
    
    // out = pop(&start, &stop);
    // printf("popped %ld\n", out);
    // printStack(&start, &stop);
    
    // out = pop(&start, &stop);
    // printf("popped %ld\n", out);
    // printStack(&start, &stop);
    
    // out = pop(&start, &stop);
    // printf("popped %ld\n", out);
    // printStack(&start, &stop);
    
    // out = pop(&start, &stop);
    // printf("popped %ld\n", out);
    // printStack(&start, &stop);
    
    // out = pop(&start, &stop);
    // printf("popped %ld\n", out);
    // printStack(&start, &stop);